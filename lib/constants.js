// Media/event categories — fixed set per design_guidelines.json. Do not add
// categories here; legacy data with an unrecognized category (e.g. the old
// "Community" tag) falls back to the Wellness color, not a new category.
export const MEDIA_CATEGORIES = ["Finance", "Fitness", "Family", "Wellness", "Training"];
